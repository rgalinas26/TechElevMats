{
  "name": "tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "./node_modules/.bin/http-server",
    "cypress:run": "./node_modules/.bin/cypress run -c video=false",
    "cypress:open": "cypress open -c video=false",
    "test": "npm run start & npm run cypress:open",
    "test-headless": "npm run start & npm run cypress:run"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^3.1.5",
    "http-server": "^0.11.1"
  }
}